package com.example.pc2

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button
import com.example.pc2.R

class ListActivity : AppCompatActivity() {



    /*private val viewModel: LigaViewModel by viewModels()
    private val adapter: AdapterLiga by lazy {
        AdapterLiga(viewModel.getLigas().value ?: emptyList())
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_list)

        recyclerView.adapter = adapter

        viewModel.getLigas().observe(this) { ligas ->
            adapter.submitList(ligas)
        }
    }*/
}